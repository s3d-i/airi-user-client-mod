plugins {
	id 'base'
}

allprojects {
	group = project.maven_group
	version = project.mod_version

	repositories {
		mavenCentral()
	}
}

tasks.register("compileClientJava") {
	dependsOn(":v1_21_1:compileClientJava")
}

tasks.register("runClient") {
	dependsOn(":v1_21_1:runClient")
}
